# Cheat Intake Notes

Use this folder for research dossiers before a cheat becomes an active YAML pack.

Worker agents may add one Markdown file per game here without touching backend code.
Only promote a game to `cheats/packs/<system>/<game>.yaml` after the backend can validate the save format and the editable fields are proven safe.
